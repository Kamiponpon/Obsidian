este es un resumen de lo que entendi del documento "Apuntes de Docker" para la tarea
## Introduccion
Docker ->(similar)-> Maquina virtual pero *mas livianos*, solo vienen con los elemntros básicos de SO

"Docker Hub" es un repositorio para tener imagenes *docker*, inicialice con github y instale su version de desktop

un *docker* es un modelo de instrucciones para construir un contenedor <=> un contenedor es una instancia en ejecución de un docer
